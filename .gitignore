# Raw microdata (large public-use files; fetched via `nhis fetch`, not committed)
data/
*.csv
*.zip

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Local secrets
.env

# Run working state (kept local, not committed product source — see Baton Run Artifacts)
.agents/

# macOS
.DS_Store

# CDK / deploy synth artifacts
deploy/infra/cdk.out/
deploy/infra/cdk.context.json
deploy/infra/node_modules/
