# Installed into the Bedrock AgentCore runtime container (arm64 / aarch64).
#
# Runtime path only: agentcore_app -> chat -> retrieval (TF-IDF) over the *already-compiled*
# .okf bundle that ships in the CodeZip. So NO pandas (build-time only) and no anthropic
# (the runtime uses Bedrock). Keep the zip <= 250 MB; if it's over, switch to a Container build.
strands-agents>=1.0
bedrock-agentcore>=1.9.1
scikit-learn>=1.3
numpy>=1.24
PyYAML>=6.0
